version: '2'

services:
  app:
    volumes:
      - ~/.cache/composer:/home/.composer/cache

#    labels:
#      traefik.frontend.rule: "Host:app.docker"

#    ports:
#      - "80:80"

#    environment:
#      - VIRTUAL_HOST=app.docker
#    networks:
#      - default
#      - proxy

#networks:
#  proxy:
#    external:
#      name: proxy
