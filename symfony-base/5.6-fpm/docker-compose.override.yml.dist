version: '2.1'

services:
    app:
        environment:
            - VIRTUAL_HOST=domain.docker
        networks:
            - default
            - proxy

networks:
    proxy:
        external:
            name: proxy
