version: '3'

services:
    app:
        build: docker/php
        image: mykiwi/symfony-base:5.6-fpm-alpine
        working_dir: /srv
        volumes:
            - .:/srv
        command: ./bin/console server:run 0.0.0.0:8000
        expose:
            - "8000"
